import type { InteractableId } from "@/types/game";

export const INTERACTION_PROMPTS: Record<InteractableId, string> = {
  "counseling-house": "문 열기",
  pbao: "말 걸기",
  "exit-house": "밖으로 나가기",
  guestbook: "방명록 작성",
  installation: "흔적 설치하기",
};
